{
  "name": "athena-fullstack-micro",
  "version": "0.1.0",
  "description": "Athena Fullstack Template",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "dev": "npx nodemon --exec ATHENA_ENV=\"development\" crystal run --watch src/root src/root/server.cr",
    "dev:all": "npx nodemon --exec ATHENA_ENV=\"development\" crystal run --watch src src/root/server.cr -D athena_all",
    "stage": "npx nodemon --exec ATHENA_ENV=\"production\" crystal run --watch src src/root/server.cr",
    "stage:all": "npx nodemon --exec ATHENA_ENV=\"production\" crystal run --watch src src/root/server.cr -D athena_all",
    "build": "shards build $1 && npm run build:assets",
    "build:all": "shards build && npm run build:assets",
    "build:assets": "npx snowpack build",
    "db": "bin/micrate $1"
  },
  "author": "Andrew Crotwell",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^2.0.7",
    "snowpack": "^3.0.11"
  }
}